// This is a template, using handlebars

const testArray = ["test1", "test2", "test3"];

{{#if enabledExtensions.graph}}
// conditionaly render a string with handlebars
const graph = "The graph is installed";
{{/if}}

{{#if enabledExtensions.another}}
// conditionaly render a string with handlebars
const another = "Another Extension is installed";
{{/if}}